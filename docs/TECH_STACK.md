# 技术栈规格

## 1. 总体架构图

```
┌─────────────────────────────────────────────────┐
│                   浏览器（Chrome）                │
│  Vue 3 + Element Plus + ECharts + Leaflet       │
│                                                  │
│  EventSource (SSE) ◄──── 实时迭代日志            │
│  Axios ◄────────────────► REST API               │
└──────────────────────┬──────────────────────────┘
                       │ HTTP
┌──────────────────────▼──────────────────────────┐
│                Flask 后端                        │
│  Blueprint 路由 │ JWT 认证 │ SSE 推送            │
│                                                  │
│  ┌────────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ 数据管理模块 │  │ 算法核心  │  │ 数据解析模块  │ │
│  │            │  │ ACO/GA/SA│  │ Solomon/Seoul │ │
│  └─────┬──────┘  └────┬─────┘  └──────┬───────┘ │
│        │              │               │          │
│        ▼              ▼               ▼          │
│  ┌─────────────────────────────────────────────┐ │
│  │              SQLite 数据库                    │ │
│  └─────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

## 2. 前端依赖清单

| 包名 | 版本 | 用途 |
|------|------|------|
| vue | ^3.4 | 前端框架 |
| vue-router | ^4.3 | 页面路由 |
| pinia | ^2.1 | 状态管理 |
| element-plus | ^2.7 | UI 组件库 |
| @element-plus/icons-vue | ^2.3 | 图标库 |
| echarts | ^5.5 | 图表可视化 |
| vue-echarts | ^7.0 | ECharts Vue 封装 |
| leaflet | ^1.9 | 地图引擎 |
| @vue-leaflet/vue-leaflet | ^0.10 | Leaflet Vue 封装 |
| axios | ^1.7 | HTTP 请求 |
| vite | ^5.4 | 构建工具 |
| @vitejs/plugin-vue | ^5.1 | Vite Vue 插件 |

## 3. 后端依赖清单

| 包名 | 版本 | 用途 |
|------|------|------|
| Flask | ^3.0 | Web 框架 |
| flask-cors | ^4.0 | 跨域支持 |
| PyJWT | ^2.8 | JWT Token 生成与校验 |
| numpy | ^1.26 | 矩阵运算、科学计算 |
| scipy | ^1.12 | 截断正态分布等统计工具 |
| ortools | ^9.9 | Google OR-Tools 精确求解器 |
| openpyxl | ^3.1 | Excel 导出（Nice-to-have） |

## 4. 算法/科学计算依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| numpy | ^1.26 | 信息素矩阵、距离矩阵、概率计算 |
| scipy | ^1.12 | 截断正态分布生成需求量 |
| ortools | ^9.9 | 锚点校验精确解求解 |

## 5. 开发工具链

| 工具 | 版本 | 说明 |
|------|------|------|
| Python | 3.11+ | 后端运行时 |
| Node.js | 20 LTS | 前端构建 |
| npm | 10+ | 前端包管理 |
| pip | 23+ | Python 包管理 |
| Git | 2.40+ | 版本控制 |

## 6. 浏览器兼容性

| 浏览器 | 最低版本 |
|--------|---------|
| Chrome | 90+ （推荐，答辩演示用） |
| Edge | 90+ |
| Firefox | 90+ |

## 7. 版本锁定说明

- 前端：通过 `package-lock.json` 锁定依赖版本
- 后端：通过 `requirements.txt` 指定精确版本（`==`）
- Python 虚拟环境：使用 `venv` 隔离项目依赖
