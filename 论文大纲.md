# MOURO 项目概述与论文大纲

## 项目概述

| 字段 | 内容 |
|------|------|
| 项目名称 | MOURO (Multi-Objective Urban Route Optimization) |
| 项目定位 | 考虑应急程度与客户需求的城市配送多目标路径优化系统 |
| 项目性质 | 南京林业大学本科毕业设计 |
| 核心问题 | 城市末端配送中，不同应急等级客户（医疗/生鲜/普通）对时效要求差异大，传统 VRP 模型无法兼顾成本、时效与惩罚的多目标协同优化 |

### 三大研究成果

1. **数学模型**：构建融合应急等级的多目标 VRPTW 模型，目标函数 Z = λ₁F1* + λ₂F2'* + λ₃F3*
2. **改进算法**：提出双信息素+双启发式+时间窗紧迫度因子+复合局部搜索的改进蚁群算法
3. **仿真系统**：开发 Vue 3 + Flask 全栈可视化决策平台，支持实时求解与多算法对比

### 技术架构

```
前端: Vue 3 + Element Plus + ECharts + Leaflet/OSM
后端: Flask + SQLite + NumPy
算法: 改进ACO / 标准ACO / 遗传算法 / 模拟退火 / OR-Tools
通信: SSE 实时推送迭代进度
```

### 数据来源

- Solomon VRPTW 标准算例（C101/C201/R101/R201/RC101/RC201）
- 首尔 ACVRP 真实路网仿真数据（非对称距离矩阵）

---

## 论文大纲

### 第1章 绪论

#### 1.1 研究背景与意义

- 城市末端配送需求激增，应急物资（医疗、生鲜）对时效要求极高
- 传统 VRP/VRPTW 模型仅优化单一目标（距离或时间），无法兼顾不同应急等级客户的差异化需求
- 多目标优化在城市配送中的实际价值：降低成本的同时保障紧急客户的服务质量
- 本课题的工程意义：为配送企业提供可视化决策支持工具

#### 1.2 国内外研究现状

- **VRP/VRPTW 经典模型**：Dantzig-Ramser (1959) 提出 VRP，Solomon (1987) 提出 VRPTW 标准算例
- **蚁群算法求解 VRP**：Dorigo (1996) 提出 ACO，后续改进包括 MMAS、ACS 等变体
- **多目标优化方法**：加权求和法、Pareto 前沿法、ε-约束法的对比
- **应急物流研究**：现有文献多关注灾后应急调度，缺少将应急等级融入日常配送 VRP 模型的研究
- **智能优化算法对比**：遗传算法、模拟退火、蚁群算法在 VRPTW 上的性能对比综述
- **研究空白**：缺乏将客户应急等级与时间窗惩罚系数动态关联的多目标 VRPTW 模型

#### 1.3 研究内容与技术路线

- 研究内容概述：数学建模 → 算法设计 → 系统实现 → 实验验证
- 技术路线图（流程图形式）
- 论文章节安排说明

### 第2章 问题描述与数学建模

#### 2.1 问题描述

- 城市末端配送场景定义：单配送中心、同质车队、容量约束 Q、无限车辆数
- 客户应急等级分类及其业务含义：
  - medical（医疗急件）：ω=2.0，硬时间窗，早到等待、迟到不可行
  - fresh（生鲜冷链）：ω=1.5，紧软时间窗，早到立即服务+惩罚
  - normal（普通快递）：ω=1.0，宽松软时间窗
- 车辆必须返回 depot，回程距离计入成本但不计入完工时间

#### 2.2 符号定义

- 集合符号：客户集 C、车辆集 K、节点集 N = C ∪ {0}
- 参数符号：d_ij（距离）、t_ij（行驶时间）、q_i（需求量）、Q（车辆容量）
- 时间窗参数：[ET_i, LT_i]、ST_i（服务时间）
- 应急参数：ω_ei（应急权重）、α_i、β_i（惩罚系数）
- 决策变量：x_ijk、t_arrive_i

#### 2.3 目标函数

- **F1（经济成本）**：F1 = Σ C_fixed × y_k + C_km × Σ d_ij × x_ijk（含回程）
- **F2'（加权完工时间）**：F2' = Σ ω_ei × t_arrive_i（不含回程）
- **F3（时间窗惩罚）**：F3 = Σ P_i(t)，其中惩罚函数按应急等级区分
  - 早到惩罚：α_i × (ET_i - t_arrive_i)
  - 迟到惩罚：β_i × (t_arrive_i - LT_i)^2（二次惩罚，迟到越多惩罚越重）
  - 惩罚系数与应急等级挂钩：α_i = α_base × ω_ei，β_i = β_base × ω_ei

#### 2.4 综合目标函数

- Min-Max 归一化：F* = (F - F_min) / (F_max - F_min)
- 加权求和：Z = λ₁F1* + λ₂F2'* + λ₃F3*，λ₁+λ₂+λ₃=1
- λ 权重的业务含义：省钱模式 / 抢时间模式 / 均衡模式 / 自定义

#### 2.5 约束条件

- 容量约束：每条路线总需求 ≤ Q
- 时间窗约束：硬时间窗（medical）不可违反，软时间窗允许违反但有惩罚
- 访问约束：每个客户恰好被访问一次
- 路线约束：每条路线从 depot 出发并返回 depot

### 第3章 改进蚁群算法设计

#### 3.1 标准蚁群算法回顾

- ACO 基本原理：信息素 + 启发式信息 → 状态转移概率
- 标准 ACO 求解 VRPTW 的流程
- 标准 ACO 的局限性：单一信息素无法同时引导成本和时效优化，容易陷入局部最优

#### 3.2 改进点一：双信息素矩阵

- τ_cost（成本信息素）：引导蚂蚁选择短距离路径
- τ_time（时间信息素）：引导蚂蚁选择快速到达路径
- 独立挥发率 ρ_cost、ρ_time，允许两个目标以不同速率遗忘历史信息
- 信息素初始值由贪心解的 F1、F2' 决定：τ0_cost = 1/F1_nn，τ0_time = 1/F2'_nn

#### 3.3 改进点二：双启发式信息

- η_cost = 1/d_ij（纯距离启发式）
- η_time = ω_j/t_ij（融入应急权重的时间启发式）
- ω_j 使 medical 客户的时间吸引力更强，引导蚂蚁优先服务高紧迫度客户
- 状态转移公式：P_ij ∝ τ_cost^α × η_cost^β × τ_time^γ × η_time^δ × tw_factor

#### 3.4 改进点三：时间窗紧迫度因子

- 动机：标准 ACO 对即将超时的客户没有特殊处理，容易产生大量迟到
- tw_factor 设计：
  - 未超时：tw_factor = 1 + 2 × max(0, 1 - slack_ratio)，截止时间越近值越大
  - 已超时（软时间窗）：tw_factor = exp(-2 × overtime_ratio)，指数衰减
- 硬时间窗（medical）迟到直接不可行（概率=0），仅对 medical 客户执行路线断开

#### 3.5 改进点四：精英蚂蚁信息素更新

- 仅用全局最优解更新信息素（而非所有蚂蚁）
- λ 加权沉积：Δτ_cost = w_cost/F1，Δτ_time = w_time/F2'
  - w_cost = λ₁，w_time = λ₂ + λ₃
  - 决策偏好直接影响信息素沉积强度，实现"偏好引导搜索"

#### 3.6 改进点五：复合局部搜索

- 四步流程：2-opt（路线内）→ relocate（路线间）→ 2-opt → 迟到修复
- **2-opt**：时间窗感知，拒绝会增加迟到数的交换
- **relocate**：first-improvement 策略，将客户从一条路线迁移到另一条的最佳位置
- **迟到修复**：仅针对 medical 客户，将迟到客户摘出并重新安置到能准时的路线

#### 3.7 其他工程优化

- 贪心解初始化：最近邻启发式提供初始信息素基准
- 早停机制：连续 patience 轮无改善则终止
- 预检机制：剔除从 depot 出发无论如何都无法在截止时间前到达的 medical 客户

### 第4章 系统设计与实现

#### 4.1 系统总体架构

- B/S 架构：前端 Vue 3 SPA + 后端 Flask REST API
- 通信方式：HTTP 请求 + SSE（Server-Sent Events）实时推送迭代进度
- 数据存储：SQLite 轻量数据库，存储算例数据与求解结果

#### 4.2 后端设计

- Flask 蓝图划分：auth（认证）、data（数据管理）、solve（求解）、benchmark（基准测试）
- 算法基类 BaseAlgorithm 设计：统一接口 solve(callback) → SolutionResult
- 五种算法实现：改进ACO、标准ACO、遗传算法、模拟退火、OR-Tools
- 数据解析器：Solomon 标准格式解析、首尔 ACVRP 数据解析
- 目标函数计算模块：build_schedule → calculate_f1/f2/f3 → calculate_z
- JWT 单管理员认证机制

#### 4.3 前端设计

- 页面结构：登录页 → 主布局（侧边栏导航）→ 数据管理 / 求解决策 / 算法对比 / 基准测试
- 核心组件：
  - RouteMap（Leaflet 地图 + OSM 瓦片，展示路线与客户标记）
  - ConvergenceChart（ECharts 折线图，展示收敛曲线）
  - KpiCard（关键指标卡片：Z/F1/F2'/F3/车辆数）
  - ScheduleTable（调度明细表：每个客户的到达时间、状态、惩罚值）
- 状态管理：Pinia Store（useAuthStore、useSolveStore、useResultStore）
- SSE 封装：EventSource 接收实时迭代进度，驱动收敛曲线动态更新

#### 4.4 前后端交互流程

- 求解流程时序图：用户配置参数 → POST /api/solve → 后端启动算法线程 → SSE 推送进度 → 返回最终结果
- 数据加载流程：选择算例 → POST /api/data/load → 解析入库 → 返回客户列表与地图数据
- 算法对比流程：选择多个算法 → 依次求解 → 前端汇总展示柱状图与收敛对比

### 第5章 实验与结果分析

#### 5.1 实验环境与参数设置

- 硬件与软件环境
- 算法参数表：蚂蚁数、迭代数、信息素挥发率、α/β/γ/δ、早停 patience 等
- 对照算法参数：标准ACO、遗传算法（种群/交叉/变异率）、模拟退火（初温/降温率）
- 每个算法独立运行 5 次取最优，保证统计可靠性

#### 5.2 数据集说明

- **Solomon 标准算例**（6 组）：
  - C 类（聚类分布）：C101（窄时间窗）、C201（宽时间窗）
  - R 类（随机分布）：R101（窄时间窗）、R201（宽时间窗）
  - RC 类（混合分布）：RC101（窄时间窗）、RC201（宽时间窗）
  - 应急等级分配规则：按客户 ID 分配 medical/fresh/normal
- **首尔 ACVRP 数据**：真实路网非对称距离矩阵，客户分布更贴近实际场景

#### 5.3 有效性验证（vs OR-Tools）

- OR-Tools 作为精确/近似最优解的锚点
- 准确率指标：accuracy = min(F1_ortools / F1_algo, F1_algo / F1_ortools) × 100%
- 6 组 Solomon 算例的准确率表格，验证改进 ACO 准确率 >80%
- 分析改进 ACO 在不同类型算例（C/R/RC、窄/宽时间窗）上的表现差异

#### 5.4 算法对比实验

- 四种算法（改进ACO / 标准ACO / 遗传算法 / 模拟退火）在 6 组算例上的对比
- 对比指标：Z 值、F1、F2'、F3、车辆数、求解时间
- 柱状图 + 表格展示
- 收敛曲线对比：改进 ACO 的收敛速度与最终解质量优势
- 统计分析：best_z、avg_z、std_z 体现算法稳定性

#### 5.5 λ 权重敏感性分析

- 三种预设模式下的结果对比：
  - 省钱模式（λ₁=0.6, λ₂=0.2, λ₃=0.2）：F1 最低，F2'/F3 较高
  - 抢时间模式（λ₁=0.2, λ₂=0.5, λ₃=0.3）：F2' 最低，F1 较高
  - 均衡模式（λ₁=0.33, λ₂=0.33, λ₃=0.34）：三个目标折中
- 分析 λ 如何通过信息素沉积权重影响搜索方向
- Pareto 近似前沿的可视化展示

#### 5.6 首尔真实路网验证

- 非对称距离矩阵对算法的影响分析
- 改进 ACO 在真实路网数据上的表现
- 与 Solomon 对称数据的结果差异讨论

#### 5.7 消融实验（可选）

- 逐一去除改进点，观察 Z 值变化：
  - 去掉双信息素 → 退化为单信息素
  - 去掉 tw_factor → 无时间窗紧迫度引导
  - 去掉复合局部搜索 → 仅靠蚁群构建解
  - 去掉精英更新 → 所有蚂蚁均更新信息素
- 量化每个改进点的贡献度

### 第6章 总结与展望

#### 6.1 研究总结

- 本文完成的三项工作回顾：
  1. 构建了融合应急等级的多目标 VRPTW 数学模型
  2. 设计了五点改进的蚁群算法（双信息素、双启发式、时间窗紧迫度因子、精英更新、复合局部搜索）
  3. 开发了 Vue 3 + Flask 全栈可视化决策平台
- 实验结论：改进 ACO 在 Solomon 标准算例上准确率 >80%，Z 值优于三种对照算法

#### 6.2 不足与展望

- 当前局限：
  - 单配送中心，未考虑多仓协同
  - 车队同质，未考虑不同车型的容量与速度差异
  - 静态求解，未考虑实时交通与动态订单
- 未来方向：
  - 引入多 depot 扩展模型
  - 结合深度强化学习自适应调参
  - 接入实时路况 API 实现动态路径规划
  - 大规模算例（500+ 客户）的并行化加速
